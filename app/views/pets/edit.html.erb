<h1>Editing Pet</h1>

<%= render 'form', pet: @pet %>

<%= link_to 'Show', pet_path(@pet), class: 'btn btn-primary' %> |
<%= link_to 'Back', client_pets_path(@pet.client_id), class: 'btn btn-primary' %>
